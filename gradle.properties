# ---- Shared across every Minecraft version ----
# Per-version values (minecraft_version, yarn_mappings, fabric_api_version,
# modmenu_version, geckolib_version) live in versions/<mc>/gradle.properties.

loader_version=0.16.10

modrinth_project=gGHT7Irr

# Archiving
archives_base_name=notchcurrency
group=net.fugginbeenus.notchcurrency
version=0.5.0

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Acknowledge Stonecutter's Groovy-DSL nag. This build is Groovy; the plugin
# recommends Kotlin DSL, but migrating is a separate job.
dev.kikugie.stonecutter.hard_mode=true
