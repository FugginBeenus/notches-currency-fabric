pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url "https://maven.fabricmc.net/" }
    }
}

plugins {
    id 'dev.kikugie.stonecutter' version '0.9.7'
}

rootProject.name = "Notch-Currency"

stonecutter {
    create(getRootProject()) {
        versions "1.20.1", "1.21.1", "1.21.11"
        vcsVersion = "1.20.1"
    }
}
