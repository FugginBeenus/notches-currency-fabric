# Wiki pages, staged

These are the GitHub wiki pages, held here until 0.12.0 is released.

The wiki is public, so publishing them early would document features nobody can download yet.

**At release:** clone `git@github.com:FugginBeenus/notches-currency-fabric.wiki.git`, copy these
four files over the top, commit and push.

- `13.-Quests.md` and `14.-Animations.md` are new pages.
- `08.-NPCs.md` gained a Reactions section, pointers to both libraries, and a corrected tab list
  (Schedule lives under Moves, not on its own tab).
- `Home.md` has the version, two overview lines and two contents entries.
